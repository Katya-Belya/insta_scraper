# Direct dependencies for the OCR prototype.
#
# Also requires the Tesseract OCR engine, which is a system binary and not a
# pip package. Install it separately and ensure it is on PATH:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   macOS:   brew install tesseract
#   Debian:  apt install tesseract-ocr

matplotlib==3.10.8
numpy==2.4.2
opencv-python==4.13.0.92
pillow==12.1.1
pytesseract==0.3.13

# Notebook environment
jupyterlab==4.5.5

# Development: test runner, and the hook runner that strips notebook outputs
pytest==9.1.1
pre-commit==4.6.1
